import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'comet-chat-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
